version: '3.8'
services:
    nocs-mentor-php:
        container_name: nocs-mentor-php
        build:
            context: ./
            dockerfile: images/php/Dockerfile
        depends_on:
            - nocs-mentor-mysql
        links:
            - nocs-mentor-mysql
        ports:
            - "8080:80"
        networks:
            - bubble
        volumes:
            - ./config/apache/default.conf:/etc/apache2/sites-enabled/default.conf
            - type: bind
              source: ./src
              target: /var/www/html
    nocs-mentor-mysql:
        container_name: nocs-mentor-mysql
        platform: linux/x86_64
        image: mysql
        restart: always
        environment:
            MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
        ports:
            - "3306:3306"
        networks:
            - bubble
        volumes:
            - ./storage/mysql:/var/lib/mysql
    nocs-mentor-phpmyadmin:
        container_name: nocs-mentor-phpmyadmin
        image: phpmyadmin
        ports:
            - "3305:80"
        restart: always
        environment:
            PMA_HOST: nocs-mentor-mysql
            PMA_PASSWORD: ""
        depends_on:
            - nocs-mentor-mysql
        networks:
            - bubble
volumes:
    src:
networks:
  bubble: